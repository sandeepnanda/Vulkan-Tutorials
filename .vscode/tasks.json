{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Building project",
            "command": "${workspaceFolder}/build.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
} 